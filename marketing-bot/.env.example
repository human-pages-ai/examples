# Human Pages API
API_URL=https://humanpages.ai

# Agent credentials (leave blank to auto-register on first run)
AGENT_API_KEY=

# Agent registration info (used only if AGENT_API_KEY is blank)
AGENT_NAME=Marketing Bot
AGENT_DESCRIPTION=Hires humans for social media promotion and marketing tasks
AGENT_CONTACT_EMAIL=bot@example.com

# Webhook server (optional — the bot works without webhooks by polling the API)
# WEBHOOK_PORT=4000
# WEBHOOK_HOST=0.0.0.0
# WEBHOOK_URL=https://your-public-url.com
# WEBHOOK_SECRET=change-me-to-a-random-string-at-least-16-chars

# ── LLM — smart replies (optional) ──
# LLM_BASE_URL=http://localhost:11434
# LLM_MODEL=llama3
# LLM_API_KEY=

# ── Owner notifications via Telegram (optional) ──
# OWNER_TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# OWNER_TELEGRAM_CHAT_ID=123456789

# ── Payment ──
#
# RECOMMENDED: Use encrypted keystore for mainnet (private key never on disk in plaintext):
#   npm run generate-keystore
#
# Alternative: Private key in env (acceptable for testing)
# WALLET_PRIVATE_KEY=0x...
#
# Network to send USDC on (default: base for Base mainnet)
PAYMENT_NETWORK=base

# ── Project being promoted ──
# Name of the project/product you're promoting
PROJECT_NAME=My Project
# URL to drive traffic to (optional)
PROJECT_URL=https://example.com
# Social media accounts for marketers to tag/mention (pipe-separated, optional)
# SOCIAL_LINKS=X/Twitter: https://x.com/YourHandle | Instagram: https://instagram.com/YourHandle | Linktree: https://linktr.ee/YourHandle

# ── Marketing task ──
ERRAND_DESCRIPTION=Promote our project on your social media channels — share posts with your honest take.
JOB_PRICE_USDC=20
